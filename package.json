{
  "name": "data_backend",
  "version": "1.0.0",
  "description": "This is backend for the fake news detection project to store to user submitted posts and images.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pranay Solse",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "multer": "^1.4.2"
  }
}
